#include<stdio.h>
#include <stdlib.h>
int main()
{
    /*
    Malloc does not initialize the memory area , the alloacted memory area will have the garbage value
    Calloc initialize the memory area to 0. 
    Calloc() = malloc() + memset(zero)
    Memset is a function which is used to fill a memory block with a particular value.

    */
    return 0;
}