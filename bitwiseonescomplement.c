#include <stdio.h>
int main()
{    //ivert the bits one to zero and vice versa
    // complement of 2 is -(2+1)
    // similarly complement of 6 is -(6+1)
    // in general complement of n = - (n+1)
    // ~(~n)=n
    


       
    return 0;
}