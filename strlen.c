#include <stdio.h>
#include <string.h>

int main()
{
    char str[] = "Anurag";
    int str_length;
    str_length = strlen(str);
    printf(" length = %d", str_length);
    return 0;
}
//Output: length = 5
